site_name: SaLinA

theme:
  name: "material"

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          setup_commands:
            - import sys
            - sys.path.append("salina")
            - sys.path.append("salina/agents")
