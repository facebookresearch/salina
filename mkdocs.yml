site_name: SaLinA

theme:
  name: readthedocs

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          setup_commands:
            - import sys
            - sys.path.append("salina")
          selection:
            new_path_syntax: true
